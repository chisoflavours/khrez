#myTopnav.topnav
  = link_to 'K', root_path, id: 'logo'
  - unless admin_signed_in?
    - unless user_signed_in?
      %a#down
        = link_to 'Sign Up', new_user_registration_path,:class => "login"
      %a{:href => "#"}= link_to 'Log In', new_user_session_path,:class => "colour"
      .not_mobile
        %a{:href => "#"}= link_to 'List Your Venue', page_listyourvenue_path,:class => "colour"
  - if admin_signed_in?
    %a= link_to('Logout', destroy_admin_session_path, method: :delete)


  - if user_signed_in?
    %a#down= link_to('Logout', destroy_user_session_path, method: :delete)
    %a= link_to "Edit Profile", edit_user_registration_path
  
  %a#hammenu.icon{:href => "javascript:void(0);", :onclick => "myFunction()", :style => "font-size:35px;padding: 15px 30px;color:#00CED1"} ☰
