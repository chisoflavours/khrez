.hero-image
  = render 'page/nav'

  .wrap
    .search
      = form_tag(venues_path, :method => "get", :id => 'search_form') do
        = select_tag((:category), options_for_select([["Hall","hall"],["Bar","bar"],["Meeting Room","meeting room"],["Reception","reception"],["Kids Party","kids party"],["Exhibition","exhibition"],["Performance Venue","Performance Venue"],["Wedding Venue","Wedding Venue"],["Private Room","private room"],["Conference Room","conference room"]],params[:category].to_i), prompt: "Occasion", class:"searchTerm2")
        .searchline
        = text_field_tag (:city), params[:city], placeholder: "Location ", required: true, :class => 'searchTerm', autocomplete: 'off'
        = button_tag '<i class="fa fa-search"></i>'.html_safe, :type => "submit", :class => "searchButton"
   
          


  .hero-text
    %h1 Find Your Perfect Venue
    %h2 As Easy As Possible

.explore_text
  %h1 
    Explore
    %br>/
    Our
    %br>/
    Venues .

  #posts
    - @venue.each do |venue|
      .post
        .post_image
          = link_to ( image_tag venue.thumbnail.url(:medium)), venue, target: :_blank
        .post_content
          = link_to venue.name, venue, :class => 'venuetext', target: :_blank
          %br/
          = link_to venue.city, venue, :class => 'venuecity', target: :_blank


/ Global Site Tag (gtag.js) - Google Analytics
%script{:async => "", :src => "https://www.googletagmanager.com/gtag/js?id=UA-106207758-1"}
:javascript
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());

  gtag('config', 'UA-106207758-1');


  // Get the modal
  var modal = document.getElementById('myModal');

  // Get the button that opens the modal
  var btn = document.getElementById("myBtn");

  // Get the <span> element that closes the modal
  var span = document.getElementsByClassName("close")[0];

  // When the user clicks the button, open the modal 
  btn.onclick = function() {
    modal.style.display = "block";
  }

  // When the user clicks on <span> (x), close the modal
  span.onclick = function() {
    modal.style.display = "none";
  }

  // When the user clicks anywhere outside of the modal, close it
  window.onclick = function(event) {
    if (event.target == modal) {
      modal.style.display = "none";
    }
  }
  
