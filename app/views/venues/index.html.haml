= render 'page/nav'

- if admin_signed_in?
  .topheader
    %h1 My Venues
- unless admin_signed_in?
  .topheader2
    %h1
      = @venue.count 
      reasults for
      #{params[:city]} #{params[:category]}

#pins
  - @venue.each do |venue|
    .box
      = link_to ( image_tag venue.thumbnail.url), venue, :target => "_blank"
      %h2= link_to venue.name, venue, :target => "_blank"
      %p.user= link_to venue.city, venue, :target => "_blank"

%br/
%br/
%br/
%br/
%br/




        


      



